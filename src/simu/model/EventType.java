package simu.model;

import simu.framework.IEventType;

/**
 * Enum class that defines the event types used in the simulation.
 */
public enum EventType implements IEventType {
    ARR, DEP, DEP1, DEP2, DEP3, DEP4
}
